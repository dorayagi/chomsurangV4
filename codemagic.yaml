workflows:
  ios-workflow:
    name: iOS Build & Upload
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build .ipa
        script: flutter build ipa --release --build-name=3.1.0 --build-number=29

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        apple_id: dorayagi@gmail.com
        api_key_id: G9A8MHP9T6
        issuer_id: 3cdd00be-36f5-4047-a99e-439c53b66d13
        api_private_key: $APP_STORE_CONNECT_PRIVATE_KEY
        submit_to_app_store: true
        distribution_type: app_store
